import 'package:flutter/material.dart';

class NewsDetailSceen extends StatelessWidget {
  const NewsDetailSceen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Text(""),
      ),
    );
  }
}
